import type { Config } from "tailwindcss";

/**
 * Tailwind v4 compatibility stub.
 * Shared tokens and plugins come from `@caliber/shared-config/styles`.
 */
const config: Config = {};

export default config;
